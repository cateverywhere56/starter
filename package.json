{
  "name": "paper-du-jour",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "generate": "node scripts/generate-paper.js"
  },
  "scripts": {
    "generate": "node scripts/generate-paper.js",
    "generate:clearecondl": "node scripts/generate-clearecondl.js",
    "generate:all": "npm run generate && npm run generate:clearecondl",
    "build": "astro build"
  },
  "scripts": {
    "generate:gehc2025": "node scripts/generate-gehealthcare-2025.js"
  },
  "dependencies": {
    "astro": "^4.10.0",
    "gray-matter": "^4.0.3",
    "xml2js": "^0.6.2",
    "node-fetch": "^3.3.2",
    "slugify": "^1.6.6"
  }
}
