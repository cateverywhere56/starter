name: Daily Paper du jour

on:
  schedule:
    - cron: '*/30 * * * *'   # toutes les 30 minutes (UTC)
  workflow_dispatch: {}
  push:
    branches: [ main ]

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install deps
        run: npm ci

      # 👉 Génération des contenus (colonne principale)
      - name: Generate news (papers)
        run: npm run generate

      # 👉 Colonne gauche : CleaReconDL
      - name: Generate CleaReconDL (left column)
        run: npm run generate:clearecondl

      # 👉 Colonne droite 1 : GE HealthCare vidéos 2025 (filtrées)
      - name: Generate GE HealthCare videos (2025)
        run: npm run generate:gehc2025
        env:
          YT_API_KEY: ${{ secrets.YT_API_KEY }}   # requis pour l'appel API

      # 👉 Colonne droite 2 (pass 1) : LinkedIn — profil ciblé (debug)
      #    - Scrape uniquement les profils (pas de SERP)
      #    - Logs verbeux + HTML sauvegardé dans .cache/li/
      #    - Throttle plus lent pour limiter les blocages
      - name: Generate LinkedIn posts (profile-focused debug)
        run: node scripts/generate-linkedin-clearecondl.js
        env:
          LOG_LEVEL: debug
          DEBUG_SAVE_HTML: "1"
          PROFILE_ONLY: "1"
          PAUSE_MS: "1800"
          PROFILE_URLS: "https://www.linkedin.com/in/charles-nutting-do-fsir-5b18b95a/"
          # (optionnel) tu peux ajouter d'autres profils, séparés par des virgules
          # PROFILE_URLS: "https://www.linkedin.com/in/charles-nutting-do-fsir-5b18b95a/,https://www.linkedin.com/in/foo/"

      # 👉 (facultatif mais utile) Publier l'artifact debug si présent
      - name: Upload LinkedIn debug HTML (if any)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: li-debug-html
          path: .cache/li
          if-no-files-found: ignore
          retention-days: 7

      # 👉 Colonne droite 2 (pass 2) : LinkedIn — collecte standard (SERP + profils)
      #    - Utilise les clés si elles existent (API Bing / SerpAPI)
      - name: Generate LinkedIn posts (CleaReconDL — full)
        run: npm run generate:li-clearecondl
        env:
          AZURE_BING_KEY: ${{ secrets.AZURE_BING_KEY }}   # 🔑 Bing Web Search API v7 (optionnel mais recommandé)
          SERPAPI_KEY: ${{ secrets.SERPAPI_KEY }}         # 🔑 SerpAPI (optionnel)

      # 🔎 Diagnostics : vérifie les fichiers générés
      - name: Debug counts
        run: |
          echo "papers:        " $(ls -1 src/content/papers 2>/dev/null | wc -l || true)
          echo "clearecondl:   " $(ls -1 src/content/clearecondl 2>/dev/null | wc -l || true)
          echo "cleareconDL:   " $(ls -1 src/content/cleareconDL 2>/dev/null | wc -l || true)
          echo "gehc2025:      " $(ls -1 src/content/gehc2025 2>/dev/null | wc -l || true)
          echo "li-clearecondl:" $(ls -1 src/content/li-clearecondl 2>/dev/null | wc -l || true)
          echo "Recent files (clearecondl):"
          ls -lt src/content/clearecondl 2>/dev/null | head -n 10 || true
          echo "Recent files (cleareconDL):"
          ls -lt src/content/cleareconDL 2>/dev/null | head -n 10 || true
          echo "Recent files (gehc2025):"
          ls -lt src/content/gehc2025 2>/dev/null | head -n 10 || true
          echo "Recent files (li-clearecondl):"
          ls -lt src/content/li-clearecondl 2>/dev/null | head -n 10 || true

      - name: Commit generated content back to repo
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: content update $(date -u +'%Y-%m-%d %H:%M') [skip ci]"
            git push
          fi

      - name: Build
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
